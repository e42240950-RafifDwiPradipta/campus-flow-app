name: print_it_bondowoso
description: "A new Flutter project."
publish_to: "none"

version: 1.0.0+1

environment:
  sdk: ">=3.10.7 <4.0.0"

dependencies:
  flutter:
    sdk: flutter
  # PENTING: intl dan lainnya harus sejajar di bawah flutter:
  intl: ^0.19.0
  table_calendar: ^3.1.2
  cupertino_icons: ^1.0.8
  file_picker: ^11.0.2
  http: ^1.6.0
  provider: ^6.1.5
  webview_flutter: ^4.13.1
  google_fonts: ^6.2.1
  image_picker: ^1.1.2
  shared_preferences: ^2.5.5
  firebase_core: ^4.9.0
  firebase_auth: ^6.5.1
  cloud_firestore: ^6.4.1

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.0

flutter:
  uses-material-design: true

  # Aktifkan jika nanti sudah ada folder assets/images
  # assets:
  #   - assets/images/
